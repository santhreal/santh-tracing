# Trusted dependencies

Each external dependency is pinned to an exact version in `Cargo.toml` and
audited before it appears here. `santh-error` is an internal path dependency,
not an external crate.

- tracing
- tracing-subscriber
- prometheus
- criterion
- proptest
