# BACKLOG - santh-tracing
src/lib.rs:82 | bug | high | Thread-local TOOL name is only set on the thread that runs init, so spawned threads get empty tool names and metrics are mis-namespaced under santh___ | FIXED: TOOL is now a process-global static RwLock<String> (was thread_local RefCell); init_with writes it, get_tool_name + with_op read it. Spawned worker threads now see the real tool name. Proving test lib.rs tool_global_tests::tool_name_is_visible_from_spawned_threads. OP_STACK stays thread-local (per-thread call nesting is correct). | status=done
src/metrics/prom.rs:33 | bug | high | Concurrency race condition allows multiple threads to overwrite the same key in METRICS with unregistered metric vectors if registry fails | FIXED: get_or_create_{counter,histogram,gauge} now hold the METRICS mutex across check+create+register+insert (was: check under lock, drop, create+register+insert under a fresh lock) so two threads can no longer both register duplicates and leave an unregistered vec in the map. Proving test prom::tests::concurrent_first_use_lands_all_increments_on_the_registered_counter (64 threads, all increments must be exported). | status=done
tests/adversarial/malformed_rust_log.rs:9 | test-gap | high | Tests in tests/adversarial.rs run in the same binary, so OnceLock INIT prevents malformed_rust_log from actually calling config.install() | Move global init state tests to separate integration test binaries | status=done | FIXED differently (better than a separate binary): the useless test asserted NOTHING and its init() was a no-op (a sibling test tripped the process-global OnceLock first, so config.install()->build_filter never ran). Root-caused the real seam: the malformed-env fallback lives in the private `InitConfig::build_filter`, which is pure w.r.t. the global subscriber. Added 4 inline unit tests (src/lib.rs `build_filter_tests`) that call build_filter DIRECTLY and assert the full contract: malformed env -> bare provided level (== level_filter(Warn), != Error); valid env directive wins over level; default_filter takes precedence over level on malformed env; deny_by_default -> "off". Each uses a UNIQUE .env_var() name (not shared RUST_LOG) so they're parallel-safe with no env races - no separate binary needed. Also rewrote the broken integration test into an honest e2e robustness smoke (init_survives_malformed_rust_log: init must not panic on `RUST_LOG=invalid=notalevel`, returns a usable guard, emitting a warn! through the resolved subscriber must not panic) with a comment that value-correctness is owned by the inline tests. Suite: 15 test binaries + 5 lib tests all green, zero warnings.
src/lib.rs:350 | bug | medium | with_op closure panic bypassed OP_STACK.pop(), polluting the thread-local op stack for future tasks on the same thread. | status=done | FIXED: added OpStackGuard whose Drop pops OP_STACK; with_op pushes then creates `let _op_guard = OpStackGuard`, so the pop runs on normal return AND on unwind (span.in_scope(body) panic). Removed the manual post-body pop. santh-tracing green.
src/metrics/prom.rs:41 | silent-fallback | medium | `let _ = registry.register(...)` swallowed registration errors -> unregistered vec silently never exports (Law 10). | status=done | FIXED: replaced all 3 `let _ = ...register(...)` (counter/histogram/gauge) with `if let Err(e) = ... { tracing::warn!(metric, error, "...registration failed; samples will not export") }`. No longer silent. The map+lock already prevents our own double-register; sanitize_metric_name (prom.rs:93 fix) removes the invalid-name cause, so this now fires only on genuine external collisions - loudly.
src/metrics/prom.rs:93 | bug | medium | santh_{tool}_{name} with a hyphenated tool (e.g. santh-cli) is an invalid Prometheus name, so CounterVec::new().ok()? returns None and EVERY metric is silently dropped. HIGH-IMPACT now that santh-cli passes real hyphenated tool names to tracing. | status=done | FIXED: added sanitize_metric_name coercing to [a-zA-Z0-9_:] (hyphens/dots -> _, colon+alnum+_ kept); counter/histogram/gauge apply it to the composed full_name. Proving tests: sanitize_metric_name_coerces_invalid_chars (exact outputs incl santh-cli->santh_cli) + hyphenated_metric_name_is_sanitized_and_exported (end-to-end: a hyphenated metric name registers and appears in the text export as underscored, raw hyphen never present).
src/redacting_writer.rs:35 | perf | medium | forward_complete_lines drained lines one-by-one from the front (each drain(..=i) shifts trailing bytes = O(N^2)). | status=done | FIXED: rposition the last newline, drain the whole complete-line block ONCE (O(N)), redact it as a single string, write once. Bonus: block redaction is strictly safer than the old per-line redaction - a multi-line PEM BEGIN...END PRIVATE KEY block within the forwarded block is now redacted (per-line scanning missed the framing and leaked the body); no single-line secret is ever missed. Proving test multiline_pem_private_key_is_redacted_as_a_block (would leak under old per-line code) + existing secret_redaction still green.
src/redacting_writer.rs:46 | perf | medium | pending buffer grows unbounded on a newline-less stream (OOM risk). | FIXED (Law-7 unbounded allocation): added a MAX_PENDING=1 MiB cap enforced by a new bound_pending_buffer() called after every write(). When a newline-less stream pushes `pending` past the cap, its head is redacted and forwarded, retaining only a raw OVERLAP=8 KiB tail so a secret straddling the forced-flush boundary is still redacted once its remainder arrives (OVERLAP is far larger than any single-line secret shape; multi-line PEM keys go through the newline path). The common newline-terminated path is untouched (no-op until the cap). Proving test tests/adversarial/secret_redaction.rs::newline_less_stream_is_bounded_and_still_redacts: writes a secret then 4 MiB of newline-less filler with NO flush; because RedactingWriter has no Drop-flush, non-empty output after drop proves the forced flush engaged (the old unbounded code would emit nothing and accumulate all 4 MiB), and the leading secret is asserted redacted ([REDACTED], raw token absent). Existing redaction + PEM-block tests stay green. RECONCILED: this row (the perf/OOM finding "pending grows unbounded") is DONE - the malformed trailing `status=open | ANALYSIS...` that co-existed here was the pre-fix design analysis and is superseded; the residual SECURITY concern it raised is split out into its own row below (src/redacting_writer.rs:46-residual) rather than left as a dangling second status on this row. | status=done
src/redacting_writer.rs:46-residual | security | medium | RESIDUAL of the MAX_PENDING force-flush fix (split out from the OOM row above): the cap redacts the flushed head then keeps only an OVERLAP=8 KiB raw tail. For NORMAL token secrets (ghp_/API keys, ~50-100 B < 8 KiB) a boundary-straddling secret is still caught once its remainder arrives. BUT a CRAFTED single-line, no-newline secret LONGER than OVERLAP can still split-leak: e.g. a `-----BEGIN PRIVATE KEY-----<4 MiB base64 with no newline>-----END-----` accumulates past MAX_PENDING, the head (BEGIN + most of the body, no END) is force-flushed, redact_secrets' PEM pattern `[\s\S]*?END` does NOT match without the END, so the base64 head is emitted UNREDACTED. Standard multi-line PEM is safe (newline path block-redacts); only the pathological single-line-unbounded case leaks. | FIX OPTIONS (product call, security-sensitive - do NOT rush): (a) fail-closed - on overflow return io::Error (never leaks, but a Write impl erroring mid-log may break the logger); (b) STATEFUL redaction - track "inside an unterminated secret opener" across force-flushes: if the head contains a BEGIN-marker (or any secret-opener) with no matching close, redact from the marker to the flush boundary and stay sticky until the close arrives (eliminates the split-leak without erroring, but needs per-pattern opener/closer metadata). Recommend (b) for the logging-writer contract. Needs a proving test: 4 MiB single-line PEM with no newline, NO flush -> assert the base64 body never appears raw in output. | FIXED: added stateful PEM redaction to RedactingWriter; secret_start tracks unterminated BEGIN markers, and forced-flush head/tail/flush paths redact from opener to matching END or to end of buffer. Test long_single_line_pem_is_redacted_across_forced_flushes verifies 4 MiB no-newline PEM body never appears raw. santh-tracing CARGO_EXIT=0. | status=done
src/error.rs:8 | stub | low | Error::SubscriberAlreadySet is defined but never constructed or returned anywhere in the library | DONE 2026-07-14: added `try_init`/`try_init_with` returning `Result<InitGuard, Error>`. `init`/`init_with` remain infallible and idempotent; `try_init` constructs `Error::SubscriberAlreadySet` on duplicate init. `cargo test` passes (CARGO_EXIT=0).
tests/concurrent/span_thread_safety.rs:15 | perf | low | Needless Box::leak of op string inside thread closure when a local borrow would satisfy the with_op signature | FIXED: with_op takes `op: &str` (any lifetime), so replaced `let op: &'static str = Box::leak(op.into_boxed_str()); with_op(op, ...)` with `with_op(&op, ...)` on the local String. No more per-thread leak. | status=done
tests/gap.rs:16 | test-gap | low | Hardcoded temp directory name gap_default_redaction without process ID can cause test collision, and files are not cleaned up | FIXED: temp dir now `santh_tracing_gap_default_redaction_{pid}` (std::process::id()), so concurrent runs cannot collide; the log is read inside a scope (guard dropped) and the temp dir removed (remove_dir_all) BEFORE the asserts, so a failing assert no longer leaves the dir behind. | status=done
DONE 2026-07-17 | src/metrics/prom.rs:56,83,110 | santh-tracing | silent-fallback | low | REPOPULATE 2026-07-16 (consolidated, one root cause x3 sites): the METRICS map Mutex lock is taken via `.ok()?`; on lock poisoning each returns None and silently disables ALL subsequent metric updates. | FIXED: all three sites now take the lock via `map.lock().unwrap_or_else(std::sync::PoisonError::into_inner)` (the map is a plain HashMap cache whose invariants survive a mid-update panic), so metrics keep flowing after a poison instead of dying silently. Proving test `counter_recovers_from_a_poisoned_metrics_lock` poisons the global METRICS mutex and asserts a subsequently recorded counter still exports. lib green exit 0. | status=done
DONE 2026-07-17 | src/metrics/prom.rs:61,88,115 | santh-tracing | silent-fallback | medium | REPOPULATE 2026-07-16 (consolidated, one root cause x3 sites): CounterVec/HistogramVec/GaugeVec::new built with `.ok()?`, so a metric with invalid label names is silently dropped and never records, with no warning. | FIXED: the three `_Vec::new(...).ok()?` sites now match on the Result and, on Err, emit a `tracing::warn!` naming the metric+error and return None (loud, not silent - Law 10). Proving test `invalid_label_metric_is_contained_not_corrupting`: a hyphenated label key (`bad-key`) makes construction fail; the call is a safe no-op (metric never exported) and does NOT panic or break subsequent valid metrics (a following valid counter still lands both increments, value==2). lib green exit 0. | status=done
src/metrics/prom.rs:133 | santh-tracing | performance | medium | REPOPULATE 2026-07-16: Every call to counter(), histogram(), or gauge() performs multiple heap allocations (cloning tool name, formatting, sanitizing, mapping labels, building keys) even if the metric vector is already cached. | FIXED 2026-07-17: added a per-thread cache (`LOCAL_CACHE`) of resolved MetricVec keyed by hash of (kind, name, label-names) and validated on hit. A hit skips the ENTIRE allocating slow path (get_tool_name clone, format!, sanitize_metric_name, label-name Vec, composed key String, global mutex) and returns a cheap Arc-clone; label VALUES use a 12-wide stack buffer (`with_label_values`) so warmed records allocate nothing. Invalidation is by a process-global `TOOL_GENERATION` (AtomicU64) bumped in the ONE-PLACE `set_tool_name` writer, so a tool-name change clears each thread's stale (mis-namespaced) entries without the hot path reading the tool string. Proving tests (thread-local slow-path counter + serial lock to avoid cross-test tool races): `warmed_cached_lookup_skips_the_allocating_slow_path` (1000 warm records take 0 slow paths; a new metric takes exactly 1 then caches; all 1002 increments export), `a_generation_change_clears_the_cache_and_forces_reresolution`, `set_tool_name_bumps_the_generation`. `cargo test -p santh-tracing --features prometheus --lib` 17 passed exit 0. | status=done
src/lib.rs:380 | santh-tracing | performance | low | REPOPULATE 2026-07-16: with_op() clones the process-global tool name and allocates/formats the entire ops_chain on every call, causing needless allocations on a hot path. | FIXED 2026-07-17: with_op now records the tool name by BORROW (`tool = %*guard` on the RwLock read guard, dropped before body runs) instead of cloning the global String every call. The ops_chain formatting was extracted into the testable pure fn `format_ops_chain(&[String])` which grows ONE String in a single pass instead of allocating one `format!` String per frame + a Vec + a join. Proving tests (`with_op_tests`): `format_ops_chain_is_empty_for_no_frames`, `_renders_one_frame_without_a_separator`, `_space_joins_nested_frames_in_order` (exact `op="outer" op="inner" op="leaf"`), `with_op_returns_the_body_value_and_pops_the_stack` (RAII pop balance). The tool-clone elimination is structural (borrow), verified by code. Full santh-tracing suite green exit 0. | status=done